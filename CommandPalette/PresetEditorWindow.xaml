<Window x:Class="CommandPalette.PresetEditorWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:CommandPalette"

        Title="Presets"
        Icon="CommandPalette.ico"

        Width="900"
        Height="620"

        WindowStartupLocation="CenterScreen"

        WindowStyle="None"
        ResizeMode="NoResize"

        Background="#171717"
        Foreground="#DEDEDE"
        PreviewKeyDown="Window_PreviewKeyDown">

    <Window.Resources>
        <SolidColorBrush x:Key="{x:Static SystemColors.HighlightBrushKey}"
                         Color="#383838"/>
        <SolidColorBrush x:Key="{x:Static SystemColors.HighlightTextBrushKey}"
                         Color="#F2F2F2"/>
        <SolidColorBrush x:Key="{x:Static SystemColors.InactiveSelectionHighlightBrushKey}"
                         Color="#303030"/>
        <Style TargetType="TextBox">
            <Setter Property="Background" Value="#242424"/>
            <Setter Property="Foreground" Value="#E5E5E5"/>
            <Setter Property="CaretBrush" Value="#FFFFFF"/>
            <Setter Property="BorderBrush" Value="#3A3A3A"/>
            <Setter Property="BorderThickness" Value="1"/>
        </Style>
        <Style TargetType="Button">
            <Setter Property="Background" Value="#292929"/>
            <Setter Property="Foreground" Value="#DDDDDD"/>
            <Setter Property="BorderBrush" Value="#424242"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="9,4"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="ButtonBorder"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="2">
                            <ContentPresenter Margin="{TemplateBinding Padding}"
                                              HorizontalAlignment="Center"
                                              VerticalAlignment="Center"
                                              RecognizesAccessKey="True"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="ButtonBorder" Property="Background" Value="#353535"/>
                                <Setter TargetName="ButtonBorder" Property="BorderBrush" Value="#555555"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="ButtonBorder" Property="Background" Value="#202020"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="ButtonBorder" Property="Opacity" Value="0.45"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style TargetType="ListBox">
            <Setter Property="Background" Value="#202020"/>
            <Setter Property="Foreground" Value="#DDDDDD"/>
            <Setter Property="BorderBrush" Value="#343434"/>
        </Style>
        <Style TargetType="ListBoxItem">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="#DEDEDE"/>
            <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#2A2A2A"/>
                </Trigger>
                <Trigger Property="IsSelected" Value="True">
                    <Setter Property="Background" Value="#383838"/>
                    <Setter Property="Foreground" Value="#FFFFFF"/>
                </Trigger>
            </Style.Triggers>
        </Style>
        <Style x:Key="DarkComboBoxItemStyle" TargetType="ComboBoxItem">
            <Setter Property="Background" Value="#242424"/>
            <Setter Property="Foreground" Value="#E5E5E5"/>
            <Setter Property="Padding" Value="7,5"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ComboBoxItem">
                        <Border x:Name="ItemBorder"
                                Background="{TemplateBinding Background}"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsHighlighted" Value="True">
                                <Setter TargetName="ItemBorder" Property="Background" Value="#343434"/>
                                <Setter Property="Foreground" Value="#FFFFFF"/>
                            </Trigger>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter TargetName="ItemBorder" Property="Background" Value="#3B3B3B"/>
                                <Setter Property="Foreground" Value="#FFFFFF"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style x:Key="DarkComboBoxStyle" TargetType="ComboBox">
            <Setter Property="Background" Value="#242424"/>
            <Setter Property="Foreground" Value="#E5E5E5"/>
            <Setter Property="BorderBrush" Value="#3A3A3A"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="7,4"/>
            <Setter Property="ItemContainerStyle"
                    Value="{StaticResource DarkComboBoxItemStyle}"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ComboBox">
                        <Grid SnapsToDevicePixels="True">
                            <Border x:Name="OuterBorder"
                                    Background="{TemplateBinding Background}"
                                    BorderBrush="{TemplateBinding BorderBrush}"
                                    BorderThickness="{TemplateBinding BorderThickness}"/>

                            <ToggleButton Focusable="False"
                                          ClickMode="Press"
                                          IsChecked="{Binding IsDropDownOpen,
                                              RelativeSource={RelativeSource TemplatedParent},
                                              Mode=TwoWay}">
                                <ToggleButton.Template>
                                    <ControlTemplate TargetType="ToggleButton">
                                        <Grid Background="Transparent">
                                            <Path Width="8"
                                                  Height="5"
                                                  Margin="0,0,9,0"
                                                  HorizontalAlignment="Right"
                                                  VerticalAlignment="Center"
                                                  Fill="#BDBDBD"
                                                  Data="M 0 0 L 4 4 L 8 0 Z"/>
                                        </Grid>
                                    </ControlTemplate>
                                </ToggleButton.Template>
                            </ToggleButton>

                            <ContentPresenter x:Name="ContentSite"
                                              Margin="7,4,29,4"
                                              VerticalAlignment="Center"
                                              IsHitTestVisible="False"
                                              Content="{TemplateBinding SelectionBoxItem}"
                                              ContentTemplate="{TemplateBinding SelectionBoxItemTemplate}"
                                              ContentTemplateSelector="{TemplateBinding ItemTemplateSelector}"/>

                            <TextBox x:Name="PART_EditableTextBox"
                                     Margin="5,2,28,2"
                                     Padding="2,0"
                                     VerticalContentAlignment="Center"
                                     Visibility="Collapsed"
                                     Background="Transparent"
                                     Foreground="{TemplateBinding Foreground}"
                                     CaretBrush="#FFFFFF"
                                     SelectionBrush="#505050"
                                     BorderThickness="0"
                                     IsReadOnly="{TemplateBinding IsReadOnly}"/>

                            <Popup x:Name="PART_Popup"
                                   Placement="Bottom"
                                   AllowsTransparency="True"
                                   Focusable="False"
                                   IsOpen="{TemplateBinding IsDropDownOpen}"
                                   PopupAnimation="Fade">
                                <Border MinWidth="{Binding ActualWidth,
                                            RelativeSource={RelativeSource TemplatedParent}}"
                                        MaxHeight="{TemplateBinding MaxDropDownHeight}"
                                        Background="#242424"
                                        BorderBrush="#4A4A4A"
                                        BorderThickness="1">
                                    <ScrollViewer CanContentScroll="True"
                                                  VerticalScrollBarVisibility="Auto">
                                        <ItemsPresenter/>
                                    </ScrollViewer>
                                </Border>
                            </Popup>
                        </Grid>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsEditable" Value="True">
                                <Setter TargetName="ContentSite"
                                        Property="Visibility"
                                        Value="Collapsed"/>
                                <Setter TargetName="PART_EditableTextBox"
                                        Property="Visibility"
                                        Value="Visible"/>
                            </Trigger>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="OuterBorder"
                                        Property="Background"
                                        Value="#2D2D2D"/>
                                <Setter TargetName="OuterBorder"
                                        Property="BorderBrush"
                                        Value="#505050"/>
                            </Trigger>
                            <Trigger Property="IsKeyboardFocusWithin" Value="True">
                                <Setter TargetName="OuterBorder"
                                        Property="BorderBrush"
                                        Value="#606060"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Opacity" Value="0.5"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style TargetType="DataGrid">
            <Setter Property="Background" Value="#202020"/>
            <Setter Property="Foreground" Value="#DEDEDE"/>
            <Setter Property="RowBackground" Value="#202020"/>
            <Setter Property="AlternatingRowBackground" Value="#242424"/>
            <Setter Property="HorizontalGridLinesBrush" Value="#303030"/>
            <Setter Property="VerticalGridLinesBrush" Value="#303030"/>
        </Style>
        <Style TargetType="DataGridColumnHeader">
            <Setter Property="Background" Value="#292929"/>
            <Setter Property="Foreground" Value="#D8D8D8"/>
            <Setter Property="BorderBrush" Value="#3A3A3A"/>
        </Style>
        <Style TargetType="DataGridRow">
            <Setter Property="Foreground" Value="#DEDEDE"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#2A2A2A"/>
                </Trigger>
                <Trigger Property="IsSelected" Value="True">
                    <Setter Property="Background" Value="#383838"/>
                    <Setter Property="Foreground" Value="#FFFFFF"/>
                </Trigger>
            </Style.Triggers>
        </Style>
        <Style TargetType="DataGridCell">
            <Setter Property="BorderBrush" Value="Transparent"/>
            <Setter Property="Foreground" Value="{Binding RelativeSource={RelativeSource AncestorType=DataGridRow}, Path=Foreground}"/>
            <Style.Triggers>
                <Trigger Property="IsSelected" Value="True">
                    <Setter Property="Background" Value="Transparent"/>
                    <Setter Property="BorderBrush" Value="#505050"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </Window.Resources>

    <Border BorderBrush="#333333" BorderThickness="1">

        <Grid>

            <Grid.RowDefinitions>
                <RowDefinition Height="44"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <Border Background="#1C1C1C"
                    BorderBrush="#303030"
                    BorderThickness="0,0,0,1"
                    MouseLeftButtonDown="TitleBar_MouseLeftButtonDown">

                <Grid>
                    <TextBlock Text="Preset editor"
                               Margin="15,0"
                               VerticalAlignment="Center"
                               FontSize="15"
                               FontWeight="SemiBold"/>
                    <Button Width="42"
                            Height="30"
                            Margin="0,0,7,0"
                            HorizontalAlignment="Right"
                            VerticalAlignment="Center"
                            Content="✕"
                            Click="Close_Click"/>
                </Grid>

            </Border>

            <Grid Grid.Row="1" Margin="18">

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="240"/>
            <ColumnDefinition Width="16"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <Grid Grid.Column="0">

            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <TextBlock
                Text="Presets"
                FontSize="20"
                FontWeight="SemiBold"
                Margin="4,0,0,12"/>

            <ListBox
                x:Name="PresetList"

                Grid.Row="1"

                Background="#202020"
                Foreground="#DDDDDD"

                BorderBrush="#353535"

                DisplayMemberPath="Name"

                SelectionChanged="PresetList_SelectionChanged"/>

            <Grid
                Grid.Row="2"
                Margin="0,12,0,0">

                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="8"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <Button
                    Grid.Column="0"

                    Height="34"

                    Content="+ New"

                    Click="NewPreset_Click"/>

                <Button
                    Grid.Column="2"

                    Height="34"

                    Content="Delete"

                    Click="DeletePreset_Click"/>

            </Grid>

        </Grid>

        <Grid
            x:Name="EditorPanel"
            Grid.Column="2">

            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <Grid Grid.Row="0">

                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="110"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <TextBlock
                    VerticalAlignment="Center"
                    Text="Id"/>

                <TextBox
                    x:Name="IdBox"

                    Grid.Column="1"

                    Height="32"

                    Padding="8,0"

                    VerticalContentAlignment="Center"/>

            </Grid>

            <Grid
                Grid.Row="1"
                Margin="0,10,0,0">

                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="110"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <TextBlock
                    VerticalAlignment="Center"
                    Text="Name"/>

                <TextBox
                    x:Name="NameBox"

                    Grid.Column="1"

                    Height="32"

                    Padding="8,0"

                    VerticalContentAlignment="Center"/>

            </Grid>

            <Grid
                Grid.Row="2"
                Margin="0,10,0,16">

                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="110"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <TextBlock
                    VerticalAlignment="Center"
                    Text="Description"/>

                <TextBox
                    x:Name="DescriptionBox"

                    Grid.Column="1"

                    Height="32"

                    Padding="8,0"

                    VerticalContentAlignment="Center"/>

            </Grid>

            <Grid Grid.Row="3">

                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <TextBlock
                    Text="Actions"
                    FontSize="16"
                    FontWeight="SemiBold"
                    Margin="0,0,0,8"/>

                <DataGrid
                    x:Name="ActionsGrid"

                    Grid.Row="1"

                    AutoGenerateColumns="False"

                    CanUserAddRows="False"

                    Background="#202020"
                    Foreground="#DEDEDE"

                    BorderBrush="#353535"

                    HeadersVisibility="Column">

                    <DataGrid.Columns>

                        <DataGridComboBoxColumn
                            Header="Type"
                            Width="180"

                            ItemsSource="{x:Static local:PresetEditorWindow.ActionTypes}"

                            DisplayMemberPath="Label"

                            SelectedValuePath="Value"

                            SelectedValueBinding="{Binding Type,
                                Mode=TwoWay,
                                UpdateSourceTrigger=PropertyChanged}">
                            <DataGridComboBoxColumn.ElementStyle>
                                <Style TargetType="ComboBox"
                                       BasedOn="{StaticResource DarkComboBoxStyle}"/>
                            </DataGridComboBoxColumn.ElementStyle>
                            <DataGridComboBoxColumn.EditingElementStyle>
                                <Style TargetType="ComboBox"
                                       BasedOn="{StaticResource DarkComboBoxStyle}"/>
                            </DataGridComboBoxColumn.EditingElementStyle>
                        </DataGridComboBoxColumn>

                        <DataGridTemplateColumn
                            Header="Application / target"
                            Width="*">

                            <DataGridTemplateColumn.CellTemplate>

                                <DataTemplate>

                                    <Grid Margin="3">

                                        <TextBox
                                            x:Name="ManualTargetBox"
                                            Text="{Binding Target,
                                                Mode=TwoWay,
                                                UpdateSourceTrigger=PropertyChanged}"
                                            Padding="6,0"
                                            VerticalContentAlignment="Center"
                                            ToolTip="Enter a URL or process name"/>

                                        <ComboBox
                                            x:Name="ApplicationPicker"
                                            Style="{StaticResource DarkComboBoxStyle}"
                                            Visibility="Collapsed"
                                            IsEditable="True"
                                            IsTextSearchEnabled="True"
                                            TextSearch.TextPath="Name"
                                            DisplayMemberPath="Name"
                                            ItemsSource="{Binding KnownApps,
                                                RelativeSource={RelativeSource AncestorType={x:Type local:PresetEditorWindow}}}"
                                            Text="{Binding Target,
                                                Mode=TwoWay,
                                                UpdateSourceTrigger=PropertyChanged}"
                                            ToolTip="Select an indexed application or type its name"/>

                                        <Grid
                                            x:Name="PathEditor"
                                            Visibility="Collapsed">

                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="*"/>
                                                <ColumnDefinition Width="8"/>
                                                <ColumnDefinition Width="80"/>
                                            </Grid.ColumnDefinitions>

                                            <TextBox
                                                Text="{Binding Target,
                                                    Mode=TwoWay,
                                                    UpdateSourceTrigger=PropertyChanged}"
                                                Padding="6,0"
                                                VerticalContentAlignment="Center"/>

                                            <Button
                                                Grid.Column="2"
                                                Content="Browse..."
                                                Tag="{Binding}"
                                                Click="BrowseActionPath_Click"/>

                                        </Grid>

                                    </Grid>

                                    <DataTemplate.Triggers>

                                        <DataTrigger Binding="{Binding Type}"
                                                     Value="{x:Static local:PresetActionType.OpenApp}">
                                            <Setter TargetName="ManualTargetBox"
                                                    Property="Visibility"
                                                    Value="Collapsed"/>
                                            <Setter TargetName="ApplicationPicker"
                                                    Property="Visibility"
                                                    Value="Visible"/>
                                        </DataTrigger>

                                        <DataTrigger Binding="{Binding Type}"
                                                     Value="{x:Static local:PresetActionType.CloseApp}">
                                            <Setter TargetName="ManualTargetBox"
                                                    Property="Visibility"
                                                    Value="Collapsed"/>
                                            <Setter TargetName="ApplicationPicker"
                                                    Property="Visibility"
                                                    Value="Visible"/>
                                        </DataTrigger>

                                        <DataTrigger Binding="{Binding Type}"
                                                     Value="{x:Static local:PresetActionType.CloseAppWindows}">
                                            <Setter TargetName="ManualTargetBox"
                                                    Property="Visibility"
                                                    Value="Collapsed"/>
                                            <Setter TargetName="ApplicationPicker"
                                                    Property="Visibility"
                                                    Value="Visible"/>
                                        </DataTrigger>

                                        <DataTrigger Binding="{Binding Type}"
                                                     Value="{x:Static local:PresetActionType.OpenPath}">
                                            <Setter TargetName="ManualTargetBox"
                                                    Property="Visibility"
                                                    Value="Collapsed"/>
                                            <Setter TargetName="PathEditor"
                                                    Property="Visibility"
                                                    Value="Visible"/>
                                        </DataTrigger>

                                    </DataTemplate.Triggers>

                                </DataTemplate>

                            </DataGridTemplateColumn.CellTemplate>

                        </DataGridTemplateColumn>

                    </DataGrid.Columns>

                </DataGrid>

            </Grid>

            <StackPanel
                Grid.Row="4"

                Margin="0,10,0,0"

                Orientation="Horizontal">

                <Button
                    Width="110"
                    Height="32"

                    Content="+ Action"

                    Click="AddAction_Click"/>

                <Button
                    Width="120"
                    Height="32"

                    Margin="8,0,0,0"

                    Content="Delete Action"

                    Click="DeleteAction_Click"/>

                <Button
                    Width="46"
                    Height="32"
                    Margin="8,0,0,0"
                    Content="↑"
                    ToolTip="Move action up"
                    Click="MoveActionUp_Click"/>

                <Button
                    Width="46"
                    Height="32"
                    Margin="6,0,0,0"
                    Content="↓"
                    ToolTip="Move action down"
                    Click="MoveActionDown_Click"/>

            </StackPanel>

            <Grid
                Grid.Row="5"
                Margin="0,18,0,0">

                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="8"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <TextBlock
                    x:Name="StatusText"

                    VerticalAlignment="Center"

                    Foreground="#909090"/>

                <Button
                    Grid.Column="1"

                    Width="100"
                    Height="36"

                    Content="Save"

                    Click="Save_Click"/>

                <Button
                    Grid.Column="3"

                    Width="100"
                    Height="36"

                    Content="Close"

                    Click="Close_Click"/>

            </Grid>

        </Grid>

            </Grid>

        </Grid>

    </Border>

</Window>
